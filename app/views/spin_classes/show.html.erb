<h2>You have joined a class!</h2>
<h4> <%= @spin_class.name %></h4>
<h5>List of all attendees:</h5>
<ol>
<% @spin_class.users.each do |user| %>
 <li><%= user.name %></li>
  <% end %>
</ol>

<p>
<%= link_to "Attending Guests", spin_class_reservations_path(@spin_class), class: "attending_guests" %>
</p>
<div class="guests">

</div>

<%= link_to('Back', spin_classes_path) %>
